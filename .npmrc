# Published manifests have `workspace:^` rewritten to a real range by
# `pnpm publish`/`pnpm pack`, so consumers installing from the registry resolve
# dsh-sync-core normally. Always publish with pnpm, never with npm.
save-exact=false
